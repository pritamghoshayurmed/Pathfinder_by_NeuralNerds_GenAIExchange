on:
  workflow_dispatch:
    inputs:
      target_repo:
        description: 'Target repository (owner/repo) to show contribution graph for'
        required: true
        default: 'pritamghoshayurmed/pathfinderAi'
  schedule:
    - cron: '0 6 * * *' # daily at 06:00 UTC (optional)

permissions:
  contents: write

jobs:
  generate-chart:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install requests python-dateutil

      - name: Generate contribution chart
        env:
          GITHUB_TOKEN: ${{ secrets.SECRET_GITHUB_TOKEN }}
          TARGET_REPO: ${{ github.event.inputs.target_repo || 'pritamghoshayurmed/pathfinderAi' }}
        run: |
          mkdir -p assets
          # convert owner/repo -> owner-repo for filename
          SAFE_TARGET="${TARGET_REPO//\//-}"
          python3 scripts/generate_contrib_chart.py \
            --target-repo "${TARGET_REPO}" \
            --out "assets/contrib-${SAFE_TARGET}.svg"

      - name: Commit and push chart (if changed)
        env:
          TARGET_REPO: ${{ github.event.inputs.target_repo || 'pritamghoshayurmed/pathfinderAi' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets || true
          if ! git diff --cached --quiet; then
            git commit -m "chore: update contribution graph for ${TARGET_REPO}"
            git push
          else
            echo "No changes to commit"
          fi
